CREATE USER 'sedemat_server'@'127.23.0.1/255.255.255.0';
CREATE USER 'sedemat_server'@'127.23.0.1/255.255.255.0';

CREATE USER 'root'@'127.0.0.3' identified by '12345';


SELECT User, Db, Host from mysql.db;


mariadb -h mariadb -u sedemat_server -p


see users with
    select User, Host from mysql.user;

to know current User
    SELECT user();

delete user with 
    DROP USER foo2@localhost,foo2@'127.%';

dar privilegios al usuario 
    GRANT ALL ON *.* TO 'user';

show tables in database:
    SHOW TABLES;

describe the table fields:
    DESCRIBE name_of_table;





no entro registrando un usuario con la ip del contenedor y el nombre de usuario test

hagamoslo usando una netmask 

si lo registro con "%", ahi si me deja ingresar

si no proporciono una ip al agregar privilegios, el usuario adquiere los privilegios 
    


mkdir sedemat_system && cd sedemat_system && git clone https://github.com/Douglas571/sedemat_system.git .
docker compose up --build